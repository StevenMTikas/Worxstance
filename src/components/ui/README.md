# UI Pattern Components

This folder holds reusable UI primitives and pattern components (e.g., cards, tabs, progress indicators)
that are styled with Tailwind CSS and can be composed inside feature modules.


