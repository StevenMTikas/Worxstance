# Library & Configuration

This folder contains low-level library and configuration modules such as:
- `firebase.ts` – Firebase initialization used by hooks and contexts
- `utils.ts` – shared utility helpers
- `types.ts` – shared TypeScript types and interfaces.


