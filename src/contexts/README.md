# Context Providers

This folder contains React context providers and hooks for shared global state such as:
- MasterProfileContext (core profile data)
- AuthContext (authentication state)
- JobTrackerContext (tracked jobs across tools).


