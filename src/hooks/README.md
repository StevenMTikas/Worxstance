# Shared Hooks

This folder contains shared React hooks such as:
- `useFirestore.ts` – wrapper for all Firestore access
- `useGemini.ts` – wrapper for Gemini LLM calls
- `useLocalStorage.ts` – small utility for persisting local UI state.


